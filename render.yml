services:
  - type: web
    name: plumber-api
    env: docker
    dockerfilePath: Dockerfile
    buildCommand: ""  # Leave empty since Dockerfile handles the build
    startCommand: "Rscript server.R"  # This is handled by Docker CMD
